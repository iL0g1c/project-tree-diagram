Cache the config file in grpc server. Update config cache when config is updated, and when config is requested immediately send it.

patrol joining command (0.5.0)
add disable command (0.5.0)
top command (kills, disable, patrol hours) (0.5.0)
patrolling get-patrols (after date and/or by user) (0.5.0)
patrolling get-patrol-hours (after date and/or by user). (0.5.0)
change all date command inputs to strings. (0.5.0)

setup so that the live and beta backends can run in dual mode.
